# 2026 지역이해 암기 퀴즈

수업 학습지(2026 지역 이해)를 지도 기반 퀴즈로 바꾼 암기 프로그램.

## 실행

### 링크로 열기 (태블릿·휴대폰 권장)

https://claude.ai/code/artifact/d48ddac4-e922-44a7-a34f-f9dbf4927a3a

아이패드·안드로이드 태블릿은 내려받은 HTML 파일을 브라우저로 여는 길이 막혀 있어
파일 방식이 통하지 않습니다. 링크로 접속하세요.
데이터를 고친 뒤에는 `python3 build.py` 로 만든 `jiri-quiz-web.html` 을 같은 주소에 다시 발행합니다.

### 파일 하나로 (PC)

**`지역이해-암기퀴즈.html`** 이 파일 하나만 받아서 더블클릭하면 됩니다.
지도까지 안에 다 들어 있어서 폴더 구조도, 인터넷도 필요 없습니다.
PC에서는 이 파일 하나만 있으면 인터넷 없이도 돌아갑니다.

### 저장소를 통째로 받았다면

`index.html`을 여세요. 단, `data/`와 `assets/` 폴더가 **같은 자리에 함께 있어야** 합니다.
index.html만 따로 빼내면 지도와 문제 데이터를 못 찾아 빈 화면이 뜹니다.

### 고친 뒤 다시 합치기

```bash
python3 build.py     # → 지역이해-암기퀴즈.html 갱신
```

## 문제 유형

| 유형 | 형태 | 예 |
|---|---|---|
| **기출형** | 아래 여섯 유형을 섞어 냅니다 | 실제 시험에 나오는 형식만 |
| 지도 → 지명 | 지도 보고 이름 타이핑 | "지도의 7번 지역의 이름은?" → `단양` |
| 특징 → 지명 | 설명 보고 지역 5지선다 | "화강암의 심층 풍화로 형성된 침식분지" → 춘천시 |
| 보기 조합 | ㄱㄴㄷㄹ 중 옳은 것만 고르기 | "충청 지방의 인구 변화로 옳은 것만을 고른 것은?" → ⑤ ㄷㄹ |
| 오류 찾기 | 네 줄짜리 표에서 틀린 줄 | "다음 표에서 설명이 옳지 않은 것은?" → ② |
| 공통점 | 여러 지역을 묶는 속성 | 강화도·인제군·태안군 → 람사르 등록 습지 |
| O·X 판별 | 붙여 준 서술이 그 지역 것인지 | "양구군 — 화강암의 심층 풍화로 형성된 침식분지" → X (춘천시) |
| 지명 → 특징 | 특징 5지선다 | "담양군에 대한 설명으로 옳은 것은?" |
| 특징 떠올리기 | 보지 않고 떠올린 뒤 확인 | "태안군의 특징을 떠올려 보세요" → 6가지 확인 |

**기출형**은 과년도 기출이 실제로 쓰는 형식만 섞습니다. '특징 떠올리기'는 기출에
없는 방향이라 여기서 빠집니다 — 시험은 특징을 나열하라고 묻지 않고,
특징을 주고 지역을 묻거나 짝이 맞는지를 묻습니다.

### 헷갈리는 짝

`data/items.js` 의 `TRAPS` — 출제자가 한 곳만 바꿔 오답을 만들 때 고르는 자리 28짝.
전 항목의 서술을 훑어 ① 같은 말을 여럿이 나눠 가진 것 ② 숫자가 겹치는 것
③ 서로 뒤집힌 개념 ④ 이름이 닮은 것을 추려 정리했습니다.

**오답 보기를 여기서 먼저 뽑습니다.** 한 자리마다 절반 남짓(55%)은 헷갈리는 짝에서,
나머지는 같은 갈래에서 가져옵니다. 전부 함정에서 뽑으면 답이 뻔해지고,
무작위로만 뽑으면 정작 위험한 짝이 잘 안 나옵니다.
문제를 푼 뒤 정답 카드 아래에 그 짝의 구분 요령이 함께 나옵니다.

### 속성 묶음

`data/items.js` 의 `GROUPS` — 같은 속성을 가진 지역을 세로로 묶은 표 20개
(화력발전소 3곳, 람사르 습지 3곳, 신기 습곡 산지 5곳 …).
'공통점' 문제의 재료이자, 요약 PDF 마지막 쪽의 '속성으로 묶어 보기' 부록입니다.
각 묶음에는 **왜 거기에 몰렸는지**(`why`)를 적어 두었습니다. 이유를 알면 목록이 짧아집니다.
'특징 → 지명'의 단서는 **그 지역에만 있는 특징**을 우선 고르고, 같은 특징을 가진
지역은 보기에서 빼서 답이 둘이 되지 않게 합니다.

- 접미어(시·군·도·강·섬·반도 등)는 있어도 없어도 정답 (`순창` = `순창군`)
- 괄호·공백 무시, 오타 1글자까지 정답 인정(정확한 표기를 함께 표시)
- 5지선다 오답 보기는 **같은 범위의 다른 지역 특징**에서 우선 추출하고,
  정답 항목이 실제로 가진 특징과 겹치는 문구는 제외

'O·X 판별'은 기출의 주력 형식(자료 속 오류 찾기 · 진술 O·X 채점)을 연습하려고 넣었습니다.
절반은 그 지역의 실제 학습지 문장을, 절반은 **같은 범위 다른 지역의 문장**을 붙이고,
틀렸을 때 그 문장의 진짜 주인이 어디인지 알려 줍니다. → [기출 분석](docs/기출-분석.md)

### 각주 내용 포함

학습지 각주는 원칙적으로 시험 범위가 아니지만, 기출 12번 ㄷ(낙동강 발원지 너덜샘 = 각주 33)처럼
출제된 사례가 있습니다. 그래서 각주 서술은 지우지 않고 `notes` 필드에 따로 두고,
홈 화면의 **'각주 내용 포함'**(14개)을 켤 때만 출제합니다. 기본값은 꺼짐입니다.

## 학습 범위 — 학습지 대단원 구조

큰 틀 두 개 아래에 학습지 단원 번호대로 놓았습니다. 단원을 통째로 고르거나,
단원 안의 세부 갈래만 골라 풀 수 있습니다.

**시험 범위는 학습지 1~11쪽과 23~34쪽입니다.** 범위 밖은 자료를 지우지 않고
홈 화면에 `범위 밖`으로 표시하며, 처음에는 선택되지 않습니다. 단원 전체가 밖일 수도
있고(005 호남), 단원 안의 한 갈래만 밖일 수도 있습니다(012 하천).
처음 켜지는 범위는 **179항목**입니다.

**한국지리 · 142항목** (이 중 시험 범위는 111항목)

| 단원 | 항목 | 세부 갈래 |
|---|---|---|
| 001 지역과 지역 구분 | 45 | 개념·용어 20 · 전통 구분 8 · 17개 시·도 17 |
| 002 수도권 | 21 | 수도권 개관 8 · 수도권 시·군 13 |
| 003 충청 지방 | 23 | 충청 개관 5 · 충청 시·군 18 |
| 004 강원 지방 | 22 | 강원 개관 9 · 강원 시·군 13 |
| 005 호남 지방 | 31 | 호남 개관 6 · 호남 시·군 25 · **범위 밖** |

**개관**은 학습지 단원 본문(범위·자연환경·산업·시기별 변화)이고,
**시·군**은 단원 끝의 지역 정리표입니다.

**세계지리 · 82항목** (이 중 시험 범위는 68항목)

| 단원 | 항목 | 세부 갈래 |
|---|---|---|
| 009 오대양 육대주 | 17 | 지구와 대륙 12 · 오대양 5 |
| 010 세계의 주요 반도와 섬 | 20 | 반도 11 · 섬 9 |
| 011 세계의 주요 바다와 호수 | 20 | 바다·만 13 · 호수 5 · 해협 2 |
| 012 세계의 산맥과 하천 | 25 | 습곡산지 2 · 산맥 9 · 하천 14 **범위 밖** |

개념·용어 · 전통 구분 · 개관은 지도가 없어 **'지도 → 지명'에서는 빠지고**
특징 고르기 · 설명 보고 맞히기 · O·X 판별 · 떠올리기로만 출제됩니다.
그 유형만 고르면 홈 화면이 몇 항목이 출제되는지 알려 줍니다.

하천 14항목은 34쪽 지도에 번호만 있고 하천별 설명은 35쪽부터라 **갈래째 범위 밖**입니다.
자료는 그대로 두었으니 홈에서 `하천` 칩을 눌러 켜면 '지도 → 지명'으로 풀 수 있습니다.

아직 데이터로 만들지 않은 단원: 006 영남 · 007 제주(15~22쪽), 013 이후(35쪽~). 모두 범위 밖입니다.

## 기능

- 대단원 단위 또는 세부 갈래 단위로 범위 선택
- 오답 우선 출제, 오답 노트 다시 풀기, 연속 3회 정답 시 숙달 처리
- 학습 기록은 브라우저에 저장(localStorage)

## 시험 범위에서 뺀 것

학습지 하단 **각주(주석)도 시험 범위입니다.** 시험 범위 안 각주 63개를 원본 문구대로
`notes` 필드에 넣어 두었고(56개 항목, 77줄), 기본으로 출제됩니다.
홈의 '각주 포함'을 끄면 본문만 출제합니다.

각주에만 정의가 있는 용어 넷(기업도시 · 경제 자유 구역 · 지리적 표시제 · 수도권 공장 총량제)은
개념·용어 항목으로 따로 만들었습니다. 기출 02번이 기업도시를 물었습니다.

## 지도

`assets/maps/` 의 지도 10장은 **학습지 원본 PDF(디지털본)에서 그대로 뽑은 것**입니다.
스캔본에서 뽑던 시절의 뭉개짐과 손글씨 비침이 없습니다.

```bash
python3 tools/extract_maps.py <학습지 원본 PDF>
```

지도 번호(①②③…)가 이미지가 아니라 PDF 텍스트로 얹혀 있는 쪽이 있어,
이미지를 꺼내지 않고 그 영역을 통째로 렌더링합니다. 잘라낼 영역은 스크립트 안에 적어 두었습니다.

17개 시·도 지도는 번호가 없어 `marker:{x,y}` 퍼센트 좌표로 위치를 찍습니다.
지도 그림이 바뀌면 이 좌표가 어긋나므로, 옛 지도와 새 지도를 겹쳐 보고 옮겨 주는 도구를 씁니다.

```bash
python3 tools/fit_markers.py <옛 지도> <새 지도> --write
```

## 전수 점검 시험지 v2 (PDF + 웹)

`docs/지역이해-점검시험-v2.pdf` — 시험 범위 학습지의 **본문과 각주 652문장**을 세 방향으로 묻는 시험지(64쪽).
웹 앱 홈의 **학습지 전수 점검 v2**가 같은 문항·같은 번호입니다. PDF에는 단원별 A·B·C와 정답으로 가는 책갈피(목차)가 있습니다.

```bash
python3 tools/make_test.py                  # data/sheet.js + PDF
python3 tools/make_test.py --src 원본.pdf   # 학습지 원본에서 빈칸 정답을 다시 뽑을 때
```

단원마다 세 부분이고, 한 문장이 A·B·C에 한 번씩 나옵니다.

| 부분 | 방향 | 문항 | 채점 |
|---|---|---|---|
| **A 빈칸 채우기** | 설명 전체 → 이름 + 빠진 말 | 카드 179 · 이름 칸 145 · 빈칸 442 | 입력 후 자동 |
| **B 특징 → 이름** | 문장 하나 → 어느 곳·무엇 | 495 | 입력 후 자동 |
| **C 이름 → 특징** | 이름(+지도 번호) → 특징 꺼내기 | 174항목 · 652가지 | 정답을 펴고 떠올린 것에 스스로 표시 |

- **A**: 지명·용어 카드는 이름 칸을 비우고, 본문에 나온 이름은 ○○로 가립니다.
  ①②③ 빈칸은 선생님이 학습지에서 비워 둔 자리를 먼저 고릅니다. 학습지 원본 PDF에 그 답이 흰 글씨로 들어 있어
  `data/blanks.json`에 뽑아 두었고, 문장마다 원본의 몇 쪽에 있는지 찾아 그 쪽의 빈칸만 씁니다.
  학습지 빈칸이 안 걸리는 문장은 다른 항목의 이름 → 숫자 순으로 비우고, 그것도 없으면 단서로만 둡니다
- **B**: 단원 안에서 순서를 섞습니다(씨앗 고정 — PDF와 웹이 같은 순서). 혼자로는 어디인지 가려지지 않는 문장
  (여러 곳이 같은 문장을 가졌거나 '인구 약 ○만 명'·'전통적 지역 구분'·'대지역 구분')은 같은 항목의 다른 문장에 붙여
  두세 줄짜리 문항으로 냅니다. 진천·음성처럼 끝까지 갈라지지 않으면 둘 다 정답입니다.
  개관 주제는 B에서 빼고 A·C로만 묻습니다
- **C**: 몇 가지인지 알려 주고 떠올리게 합니다. 웹에서는 적어 보고(선택) '정답 펴기' → 떠올린 것을 눌러 표시
- 웹 채점은 띄어쓰기·가운뎃점·괄호 속 말을 따지지 않고, 4글자 이상은 한 글자 오타를 봐줍니다.
  틀리면 오답 노트에도 들어가고, 다 푼 부분을 다시 누르면 틀린 것만 나옵니다

## 요약 PDF (굿노트용)

`docs/지역이해-요약.pdf` — **시험 범위만** 추린 17쪽짜리 요약본(8단원 179항목).

```bash
python3 tools/make_summary.py
```

- 단원 → 갈래 → 항목 순으로, 지도가 있는 갈래는 지도를 먼저 싣고 번호별 목록을 붙입니다
- 본문 아래에 학습지 **각주**를 들여써서 함께 싣습니다 (`각주` 표시)
- 마지막 두 쪽은 부록 — **헷갈리는 짝** 28짝, **속성으로 묶어 보기** 20묶음
- 범위 밖은 빠집니다 — 단원 전체(005 호남)도, 단원 안의 한 갈래(012 하천)도
- 글자가 벡터로 남아 굿노트에서 확대해도 깨지지 않고 검색도 됩니다
- HTML을 만들어 크로미움 헤드리스로 인쇄합니다. 나눔고딕은 길이가 0인 VTT 표가 있어
  크로미움이 통째로 거부하므로, 쓰는 글자만 남긴 부분집합을 만들어 심습니다

## 정답 지도 PDF (굿노트용)

`docs/지역이해-정답지도.pdf` — 학습지 항목을 지도 위에 이름과 특징까지 적어 놓은 정답본.

> 아직 **012 세계의 산맥과 하천(산맥 9 · 하천 14)** 은 이 PDF에 없습니다.
> 이 중 하천은 시험 범위 밖입니다.
> `tools/places.py` 에 좌표를 넣어야 들어갑니다.

- A4 세로 9쪽 (세계 4쪽 + 한국 5쪽) — 위쪽에 지도, 아래쪽에 지역별 특징
- 완전 벡터라 굿노트에서 아무리 확대해도 깨지지 않고, 글자 검색도 됩니다
- 지명은 4색(파랑·주황·초록·자홍)으로 구분합니다. 가까이 붙은 지명끼리는
  반드시 다른 색이 되도록 배정해, 어느 이름이 어느 점인지 바로 보입니다.
  하단 목록의 이름 앞 점도 지도와 같은 색입니다
- 지도는 공개 자료로 새로 그렸습니다 (학습지 스캔본이 아님)
  - 세계: Natural Earth 국경 + GSHHG 해안선
  - 국내: 통계청 SGIS 기반 행정 경계 (시·도 굵은 선 / 시·군·구 가는 선)

고치거나 다시 만들 때:

```bash
pip install matplotlib "geopandas<1.0" koreanize-matplotlib basemap basemap-data-hires shapely
ln -sfn <site-packages>/mpl_toolkits/basemap_data <site-packages>/mpl_toolkits/basemap/data
python3 tools/fetch_boundaries.py    # 국내 행정 경계 (한 번만)
python3 tools/make_answer_maps.py
```

좌표는 `tools/places.py`, 특징은 `data/items.js`에서 가져옵니다.

## 구성

```
index.html                  앱 전체 (UI + 퀴즈 엔진)
data/items.js               문제 데이터 224항목 · 각주 77줄 · 묶음 20 · 헷갈리는 짝 28
data/sheet.js               전수 점검 시험지 A·B·C 문항 (tools/make_test.py 가 생성)
data/blanks.json            학습지 원본의 빈칸 정답(흰 글씨) 쪽별 목록
assets/maps/*               학습지 원본 PDF에서 뽑은 지도 10장
tools/extract_maps.py       원본 PDF에서 퀴즈용 지도 추출
tools/make_summary.py       시험 범위 요약 PDF 생성
tools/make_test.py          전수 점검 시험지 v2(data/sheet.js + PDF) 생성
tools/fit_markers.py        지도가 바뀌었을 때 마커 좌표 옮기기
tools/make_answer_maps.py   정답 지도 PDF 생성
tools/places.py             지도에 찍을 곳의 위도·경도
tools/fetch_boundaries.py   국내 행정 경계 내려받기·간략화
data/geo/                   시·도 17개 · 시·군·구 250개 경계
docs/지역이해-요약.pdf       시험 범위 요약 (A4 세로 17쪽)
docs/지역이해-점검시험-v2.pdf 전수 점검 시험지 A·B·C + 정답 (A4 세로 64쪽)
docs/지역이해-정답지도.pdf   굿노트용 정답 지도 (A4 세로 9쪽)
docs/PRD.md                 제품 요구사항 정의서
docs/빈칸-정답-정리.md       빈칸 추론 내역과 원본 PDF 대조 결과
docs/기출-분석.md            과년도 기출 9쪽 분석(선택형 27 + 서답형 7)
```

## 데이터 추가

`data/items.js`의 `ITEMS` 배열에 객체를 하나 넣으면 됩니다.

```js
{ id:"yn01", map:"yeongnam", num:1, scope:"korea", category:"yeongnam",
  name:"포항시", aliases:["포항"],
  features:["제철 공업 발달","경상북도 인구 1위 도시"] }
```

번호가 인쇄된 지도는 `num`만, 백지도는 `marker:{x,y}`(백분율)를 씁니다.
새 단원은 같은 파일의 `UNITS`에, 세부 갈래는 `CATS`에 한 줄씩 추가하고
`index.html`의 `KIND`에 서답형 문제문에 쓸 낱말을 넣습니다.

## 반영 범위

학습지 1~14쪽(001~005단원), 23~31쪽(009~011단원).
